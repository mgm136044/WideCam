.build/
.DS_Store
*.xcodeproj
.swiftpm/
DerivedData/
*.app

# 로컬 서명 신원(개인 인증서 이름). deploy.sh가 읽는다. 절대 커밋하지 않는다.
.sign-identity

# 작업 기록(브리프·리포트·리뷰 diff). 지금까지는 .superpowers/sdd/.gitignore의 `*`
# 한 줄이 막고 있었지만 그 파일은 자기 자신도 무시해 추적되지 않으므로, 클론한
# 저장소에는 규칙이 하나도 없다. 루트에서 디렉터리 전체를 막는다.
.superpowers/

.vscode/
.idea/
