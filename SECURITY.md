# 보안 정책

WideCam은 맥에 연결된 아이폰 카메라와, 녹화하는 동안에만 아이폰 마이크를 사용한다. 찍은
사진과 영상은 `~/Pictures/WideCam/`에 로컬로만 저장한다. 네트워크 기능이 없고 외부
의존성도 없다 — 어떤 데이터도 수집하거나 전송하지 않는다. 앱이 읽는 시스템 정보는 카메라
목록과 맥의 Wi-Fi 전원 상태(꺼져 있으면 안내를 띄우기 위해)뿐이며, Wi-Fi 이름(SSID)이나
위치 정보는 읽지 않는다. 배포 번들은 본인 인증서로 서명해 설치하는 로컬 앱이고 App
Sandbox와 하드닝 런타임을 적용하지 않는다(README "알려진 제약" 참고).

취약점을 발견하면 이 저장소의 GitHub Issues로 알려주기 바란다. 개인 프로젝트이므로 정해진
응답 기한(SLA)은 없고, 확인하는 대로 처리한다. 심각한 내용이라면 공개 이슈에 재현 절차를
전부 적기보다 먼저 연락할 수 있는 창구를 물어봐 주면 좋다.
